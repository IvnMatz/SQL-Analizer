create table hola(
    id_user int primary key,
    username varchar(30)
)

create table hola2(
    id_gen int primary key,
    username2 varchar(40)
)

create table hola3(
    id_g int primary key,
    username3 varchar(40),
    user boolean,
    lol int
)

create table nig(
    id_nig int primary key,
    nigName varchar(30)
)